<%= form_for :befriend, url: "/befriend/user", action: :create do |f| %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :friend_id, value: user_id %>
    <%= f.submit "Befriend <3" %>
<% end %>